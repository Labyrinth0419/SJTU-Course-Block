<?xml version="1.0" encoding="utf-8" ?>
<!-- 近日课程：双栏布局（今天 | 明天），课程卡片通过 addView() 动态填充 -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_today_bg"
    android:orientation="vertical"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="6dp"
    android:paddingBottom="8dp"
>

    <!-- 头部：课表名（左）+ 日期周次（右） -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
    >

        <TextView
            android:id="@+id/tv_header"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="近日课程"
            android:textColor="@color/widget_header_text"
            android:textSize="12sp"
            android:textStyle="bold"
        />

        <TextView
            android:id="@+id/tv_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="近3天"
            android:textColor="@color/widget_subtitle_text"
            android:textSize="10sp"
        />

    </LinearLayout>

    <!-- 横向分割线 -->
    <TextView
        android:id="@+id/divider_top"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="3dp"
        android:background="@color/widget_divider"
    />

    <!-- 双栏主体 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
    >

        <!-- 今天栏 -->
        <LinearLayout
            android:id="@+id/col_today"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingEnd="4dp"
        >

            <TextView
                android:id="@+id/lbl_today"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="今天"
                android:textColor="@color/widget_header_text"
                android:textSize="10sp"
                android:textStyle="bold"
                android:paddingBottom="2dp"
            />

            <LinearLayout
                android:id="@+id/col_today_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
            />

        </LinearLayout>

        <!-- 纵向分割线 -->
        <TextView
            android:id="@+id/divider_center"
            android:layout_width="1dp"
            android:layout_height="match_parent"
            android:background="@color/widget_divider"
        />

        <!-- 明天栏 -->
        <LinearLayout
            android:id="@+id/col_tmr"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="4dp"
        >

            <TextView
                android:id="@+id/lbl_tmr"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="明天"
                android:textColor="@color/widget_subtitle_text"
                android:textSize="10sp"
                android:paddingBottom="2dp"
            />

            <LinearLayout
                android:id="@+id/col_tmr_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
            />

        </LinearLayout>

    </LinearLayout>

    <!-- 底部分割线 -->
    <TextView
        android:id="@+id/divider_bottom"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginBottom="4dp"
        android:background="@color/widget_divider"
    />

    <!-- 底部操作行：刷新（左）+ 打开（右） -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
    >

        <TextView
            android:id="@+id/btn_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="↻"
            android:textColor="@color/widget_subtitle_text"
            android:textSize="14sp"
            android:padding="2dp"
        />

        <TextView
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1"
        />

        <TextView
            android:id="@+id/btn_open"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="打开 →"
            android:textColor="@color/widget_subtitle_text"
            android:textSize="10sp"
            android:padding="2dp"
        />

    </LinearLayout>

</LinearLayout>
